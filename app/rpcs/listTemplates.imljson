{
    "url": "/templates",
    "method": "GET",
    "qs": { "per_page": 100 },
    "response": {
        "iterate": "{{body}}",
        "output": { "label": "{{item.name}}", "value": "{{item.id}}" }
    }
}
